---
title: Runtime
---

import AlphaCallout from '/snippets/alpha-lc-callout.mdx';

<AlphaCallout />

## Overview

LangChain's `create_agent` runs on LangGraph's runtime under the hood.
LangGraph exposes a static `Runtime` object with the following information:

1. Context: static information like user id, db connections, or other dependencies for an agent invocation
2. Store: a `BaseStore` instance used for long term memory
3. Stream writer: an object used for streaming information via the `custom` stream mode

## Access

When creating an agent with `create_agent`, you can specify a `context_schema` to define
the structure of the `context` stored in the agent runtime.

You can then access the runtime information within [tools](#inside-tools), [prompt](#inside-prompt), and [pre and post model hooks](#inside-pre-and-post-model-hooks).

When invoking the agent, pass the `context` argument with the relevant configuration for the run:

```python
from dataclasses import dataclass

from langgraph.runtime import get_runtime
from langchain_core.messages import AnyMessage
from langchain.agents import create_agent

@dataclass
class Context:
    user_name: str 

agent = create_agent(
    model="openai:gpt-5-nano",
    tools=[...],
    context_schema=Context
)

agent.invoke({"messages": [{"role": "user", "content": "What's my name?"}]}, context={'user_name': "John Smith"})
```

### Inside tools

Inside tools, you may want to access the runtime information to:

* Access the context
* Read or write long term memory
* Write to the stream (ex, tool progress / updates)

Use the `get_runtime` function from `langgraph.runtime` to access the `Runtime` object inside a tool.

```python
from typing import Any

from langchain_core.tools import tool

@tool
def fetch_user_email_preferences() -> str:
    runtime = get_runtime(Context)
    user_id = runtime.context.user_id

    preferences: "The user prefers you to write a concise and polite email."
    if runtime.store:
        if memory := runtime.store.get(("users",), user_id):
            preferences = memory.value["preferences"]
    
    return preferences
```

### Inside prompt

Use the `get_runtime` function from `langgraph.runtime` to access the `Runtime` object inside a prompt function.

```python
from dataclasses import dataclass

from langgraph.runtime import get_runtime
from langchain_core.messages import AnyMessage
from langchain.agents import create_agent

@dataclass
class Context:
    user_name: str

def my_prompt(state: State) -> list[AnyMessage]:
    runtime = get_runtime(Context)
    system_msg = f"You are a helpful assistant. Address the user as {runtime.context.user_name}."
    return [{"role": "system", "content": system_msg}] + state["messages"]

agent = create_agent(
    model="openai:gpt-5-nano",
    tools=[...],
    prompt=my_prompt,
    context_schema=Context
)

agent.invoke({"messages": [{"role": "user", "content": "What's my name?"}]}, context={'user_name': "John Smith"})
```

### Inside pre and post model hooks

To access the underlying graph runtime information in a pre or post model hook, you can:

1. Use the `get_runtime` function from `langgraph.runtime` to access the `Runtime` object inside the hook
2. Inject the `Runtime` directly via the hook signature

<Tabs>
	<Tab title="Using get_runtime">
		```python
		from langgraph.runtime import get_runtime

		def hook(state: State) -> State:
		    runtime = get_runtime(Context)
            ...
		```
	</Tab>
	<Tab title="Injection">
		```python
		from langgraph.runtime import Runtime

		def hook(state: State, runtime: Runtime[Context]):
		    ...
		```
	</Tab>
</Tabs>
